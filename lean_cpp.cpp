# include <iostream>
# include <string>
# include <iomanip>
# include <limits>
# include <string.h>
# include <cstring>
# include <stdio.h>
# include <vector>
# include <cmath>
// typedef std::vector<std::pair<std::string, int>> pairlist_t;
//typedef std::string text_t;
//typedef int number_t;


// we can also use (using) inside of typedef
// using text_t = std::string;
// using number_t = int;


int main(){
    // Typedef = reversed keyword used to create an additional name (alias) for another dara type.
    // help with redability and reduce the tyepos
    //std::vector<std::pair<std::string, int>> pairlist;
    // text_t Name = "Ayman";
    // number_t age {16};
    // std::cout << "My name is: " << Name << " and my age is: " << age;

    // Now we gonna see the type conversion
    // implixit and Explicit
    /*
    int x = 3.14;

    std::cout << x << std::endl;

    double y = (int) 3.14;
    std::cout << y << std::endl;

    char z = 100;
    std::cout << z << std::endl;

    std::cout << (char) 100 << std::endl;

    

    int correct_answer = 5;
    int questions = 10;

    double scores {correct_answer / (double) questions * 100};

    std::cout << "Your Score is: " << scores << "%" << std::endl;


    std::string fullname;
    int age;

    std::cout << "Fullname: " << std::endl;
    std::cout << "Age: " << std::endl;
    std::getline(std::cin, fullname);
    std::cin >> age;

    std::cout << "Is Your Name: " << fullname << " And Your Age is: " << age << std::endl;

   

   double m = 6;
   double n = 2;
   double z;

   z = std::max(m, n);
   z = pow(2, 2);
   z = sqrt(16);
   z = abs(-6);
   z = round(4.21);
   z = ceil(4.21);
   z = floor(7.43);

   std::cout << z << std::endl;



   

   char op;
   double num1;
   double num2;
   double res;

   std::cout << "**************** CALCULATOR ****************" << std::endl;

   std::cout << "Enter A Valid Operator: " << '\n',
   std::cin >> op;
   std::cout << "Enter #1: " << '\n';
   std::cin >> num1;
   std::cout << "Enter #2: " << '\n';
   std::cin >> num2;

   
   
   

   switch(op){
        case '+':
            res = num1 + num2;
            break;
        case '*':
            res = num1 * num2;
            break;
        case '-':
            res = num1 - num2;
            break;
        case '/':
            if (num2){
                res = num1 / num2;
                break;
            }else {
                std::cout << "Zero devision Error" << std::endl;
                break;
            }

   }
   std::cout << "The Result is: " << res << std::endl;

   std::cout << "*********************************************" << std::endl;
    */


    
   
}